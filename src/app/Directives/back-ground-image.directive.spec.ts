import { BackGroundImageDirective } from './back-ground-image.directive';

describe('BackGroundImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BackGroundImageDirective();
    expect(directive).toBeTruthy();
  });
});
